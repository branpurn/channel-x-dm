{
  "id": "x-dm",
  "name": "X DM",
  "description": "Send and receive X Direct Messages via the X API (classic unencrypted DM, plus opt-in X Chat).",
  "activation": { "onStartup": true },
  "channels": ["x-dm"],
  "configSchema": { "type": "object", "additionalProperties": false, "properties": {} },
  "channelConfigs": {
    "x-dm": {
      "schema": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean" },
          "transport": { "type": "string", "enum": ["classic","chat"], "default": "classic" },
          "dmPolicy": { "type": "string", "enum": ["pairing","allowlist","open","disabled"], "default": "allowlist" },
          "allowFrom": { "type": "array", "items": { "type": "string" } },
          "groupAllowFrom": { "type": "array", "items": { "type": "string" } }
        },
        "additionalProperties": false
      },
      "label": "X DM",
      "description": "X Direct Messages via the X API. transport=classic (default, unencrypted DM) or transport=chat (opt-in X Chat API)."
    }
  }
}
